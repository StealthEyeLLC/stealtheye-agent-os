import { describe, expect, it } from "vitest";
import { AppHostToolNameSchema } from "@stealtheye/app-host";
import { MCP_RUNTIME_SAFE_TOOL_NAMES, RUNTIME_RESOURCE_URIS, assertRuntimeToolInventorySafe, createDevModeConnectionInfo, createLocalDevTransport, createMcpRuntimeServer, getRuntimeCapabilities, getRuntimeHealth, listRuntimeResources, listRuntimeTools, readRuntimeResource } from "../src";
const repoToolNames = ["repo.status.summary", "repo.metadata.read", "repo.branch.read", "repo.pr.list", "repo.pr.read", "repo.issue.list", "repo.issue.read", "repo.workflow.status.read", "repo.file.metadata.read", "repo.file.content.read"] as const;
const memoryToolNames = ["memory.graph.preview", "memory.node.search.preview", "memory.context.preview", "memory.retention.preview"] as const;
const mutationForbiddenName = /delete|commit|create|update|write|rerun|dispatch|deploy|payment|purchase|browser\.click|browser\.type|production_url/i;
const mutationForbiddenEffect = /purchase|payment|deploy|commit_branch|write_branch_files|open_pr|update_pr|update_issue|rerun_ci|dispatch_workflow|mutate_repo_settings|read_secrets|read_private_repo|browser_click|credentialed|durable_memory_write|vector_database_write/i;
const secretLike = new RegExp(["AKIA[0-9A-Z]{16}", "BEGIN (RSA|OPENSSH|EC|DSA) " + "PRIVATE KEY", "client_" + "secret", "PRIVATE" + "KEY", "restricted_marker", "credit" + " card"].join("|"), "i");
function receiptOf(result: { receipt?: unknown }) { expect(result.receipt).toBeTruthy(); return result.receipt as { schema_version?: string; receipt_replay_event?: unknown; public_safe?: boolean; no_live_side_effects?: boolean; output_summary?: string; no_write_no_side_effect_statement?: string; target_repo?: string }; }
describe("MCP Runtime local/dev and repo read-only surface", () => {
  it("runtime health returns Build 12 read-only status", () => { const health = getRuntimeHealth(); expect(health.status).toBe("ok"); expect(health.local_dev_only).toBe(true); expect(health.production_ready).toBe(false); expect(health.live_write_capabilities).toBe(false); expect(health.live_read_capabilities).toBe(true); });
  it("runtime capabilities list safe tool families", () => { const capabilities = getRuntimeCapabilities(); expect(capabilities.tool_families).toEqual(["guard", "host", "memory", "mission", "policy", "registry", "repo"]); expect(capabilities.read_only_tools).toBe(true); expect(capabilities.live_write_tools).toBe(false); expect(capabilities.repo_status_read_only_tools).toEqual([...repoToolNames]); expect(capabilities.memory_graph_preview_tools).toEqual([...memoryToolNames]); });
  it("tool listing includes expected App Host, repo/status, and memory tools", () => { const names = listRuntimeTools().map((tool) => tool.name); for (const name of MCP_RUNTIME_SAFE_TOOL_NAMES) expect(names).toContain(name); for (const name of repoToolNames) expect(names).toContain(name); for (const name of memoryToolNames) expect(names).toContain(name); });
  it("tool descriptors are read-only and never live-write/destructive", () => { expect(assertRuntimeToolInventorySafe(listRuntimeTools())).toBe(true); for (const tool of listRuntimeTools()) { expect(tool.annotations.readOnlyHint).toBe(true); expect(tool.safety.readOnly).toBe(true); expect(tool.safety.liveWrite).toBe(false); expect(tool.safety.externalSideEffect).toBe(false); expect(tool.safety.destructive).toBe(false); if (tool.repo_status_tool) expect(tool.safety.liveCapabilityStatus).toBe("read_only_live"); else if (tool.memory_graph_tool) expect(tool.safety.liveCapabilityStatus).toBe("fixture_only"); else expect(tool.safety.liveCapabilityStatus).toBe("disabled"); } });
  it("no live write/destructive/money/prod/browser/repo mutation tools are exposed", () => { for (const tool of listRuntimeTools()) { expect(tool.name).not.toMatch(mutationForbiddenName); expect(tool.safety.allowedEffects.join(" ")).not.toMatch(mutationForbiddenEffect); expect(tool.safety.liveWrite).toBe(false); expect(tool.safety.destructive).toBe(false); } });
  it("host.health call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("host.health"); expect(result.ok).toBe(true); expect((result.structuredContent as { status: string }).status).toBe("ok"); expect(receiptOf(result).no_live_side_effects).toBe(true); });
  it("repo.status.summary call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("repo.status.summary"); expect(result.ok).toBe(true); expect((result.structuredContent as { result: { summary: string } }).result.summary).toContain("StealthEyeLLC/stealtheye-agent-os"); expect(receiptOf(result).target_repo).toBe("StealthEyeLLC/stealtheye-agent-os"); });
  it("memory.graph.preview succeeds", async () => { const result = await createMcpRuntimeServer().callTool("memory.graph.preview"); expect(result.ok).toBe(true); expect(result._meta.fixture_only).toBe(true); expect((result.structuredContent as { graph: { fixture_only: boolean; live_writes_enabled: boolean; durable_storage_enabled: boolean; embeddings_enabled: boolean } }).graph.fixture_only).toBe(true); expect((result.structuredContent as { graph: { live_writes_enabled: boolean } }).graph.live_writes_enabled).toBe(false); });
  it("memory.node.search.preview succeeds", async () => { const result = await createMcpRuntimeServer().callTool("memory.node.search.preview", { tag: "build-12", min_confidence: 0.8 }); expect(result.ok).toBe(true); expect((result.structuredContent as { count: number }).count).toBeGreaterThan(0); });
  it("memory.context.preview succeeds", async () => { const result = await createMcpRuntimeServer().callTool("memory.context.preview", { root_node_id: "mission:fixture:1" }); expect(result.ok).toBe(true); expect((result.structuredContent as { packet: { fixture_only: boolean } }).packet.fixture_only).toBe(true); });
  it("memory.retention.preview succeeds", async () => { const result = await createMcpRuntimeServer().callTool("memory.retention.preview"); expect(result.ok).toBe(true); expect((result.structuredContent as { report: { no_delete_performed: boolean } }).report.no_delete_performed).toBe(true); });
  it("repo write and live memory mutation names are rejected", async () => { await expect(createMcpRuntimeServer().callTool("repo.commit.live")).rejects.toThrow(/Unknown runtime tool/); await expect(createMcpRuntimeServer().callTool("repo.pr.create")).rejects.toThrow(/Unknown runtime tool/); await expect(createMcpRuntimeServer().callTool("memory.write.live")).rejects.toThrow(/Unknown runtime tool/); });
  it("invalid input is rejected", async () => { await expect(createMcpRuntimeServer().callTool("mission.validate", { fixture: "not_a_fixture" })).rejects.toThrow(); });
  it("resource listing includes runtime and dev-mode resources", () => { const uris = listRuntimeResources().map((resource) => resource.uri); expect(uris).toContain(RUNTIME_RESOURCE_URIS.runtime); expect(uris).toContain(RUNTIME_RESOURCE_URIS.devMode); expect(uris).toContain(RUNTIME_RESOURCE_URIS.health); });
  it("read resource returns public-safe content", () => { const runtime = readRuntimeResource(RUNTIME_RESOURCE_URIS.runtime); expect(runtime.public_safe).toBe(true); expect((runtime.contents as { live_write_tools: boolean }).live_write_tools).toBe(false); });
  it("tool call receipt preview is generated", async () => { const result = await createMcpRuntimeServer().callTool("memory.graph.preview"); expect(receiptOf(result).schema_version).toBe("stealtheye-mcp-runtime-receipt-preview.v1"); expect(receiptOf(result).receipt_replay_event).toBeTruthy(); });
  it("dev-mode connection info is fixture/public-safe", () => { const info = createDevModeConnectionInfo(); expect(info.public_safe).toBe(true); expect(info.production_deployment).toBe(false); expect(info.oauth_credentials_provisioned).toBe(false); expect(info.live_write_tools).toBe(false); });
  it("transport exposes local/dev handlers without requiring network access", async () => { const transport = createLocalDevTransport(); expect(transport.local_dev_only).toBe(true); expect((await transport.handle({ method: "GET", path: "/health" })).status).toBe(200); expect((await transport.handle({ method: "POST", path: "/tools/call", body: { name: "memory.graph.preview" } })).status).toBe(200); expect((await transport.handle({ method: "POST", path: "/tools/call", body: { name: "repo.metadata.read" } })).status).toBe(200); });
  it("no real secret-like material or live write endpoints appear in fixtures/descriptors", () => { const serialized = JSON.stringify({ tools: listRuntimeTools(), resources: listRuntimeResources(), devMode: createDevModeConnectionInfo() }); expect(serialized).not.toMatch(secretLike); for (const name of MCP_RUNTIME_SAFE_TOOL_NAMES) expect(AppHostToolNameSchema.safeParse(name).success).toBe(true); });
});
