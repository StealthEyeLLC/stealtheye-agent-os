import { describe, expect, it } from "vitest";
import { AppHostToolNameSchema } from "@stealtheye/app-host";
import { MCP_RUNTIME_SAFE_TOOL_NAMES, RUNTIME_RESOURCE_URIS, assertRuntimeToolInventorySafe, createDevModeConnectionInfo, createLocalDevTransport, createMcpRuntimeServer, getRuntimeCapabilities, getRuntimeHealth, listRuntimeResources, listRuntimeTools, readRuntimeResource } from "../src";
const repoToolNames = ["repo.status.summary", "repo.metadata.read", "repo.branch.read", "repo.pr.list", "repo.pr.read", "repo.issue.list", "repo.issue.read", "repo.workflow.status.read", "repo.file.metadata.read", "repo.file.content.read"] as const;
const mutationForbiddenName = /delete|commit|create|update|write|rerun|dispatch|deploy|payment|purchase|browser\.click|browser\.type|production_url/i;
const mutationForbiddenEffect = /purchase|payment|deploy|commit_branch|write_branch_files|open_pr|update_pr|update_issue|rerun_ci|dispatch_workflow|mutate_repo_settings|read_secrets|read_private_repo|browser_click|credentialed/i;
const secretLike = new RegExp(["AKIA[0-9A-Z]{16}", "BEGIN (RSA|OPENSSH|EC|DSA) " + "PRIVATE KEY", "client_" + "secret", "PRIVATE" + "KEY", "customer_" + "ssn", "credit" + " card"].join("|"), "i");
function receiptOf(result: { receipt: unknown }) { return result.receipt as { schema_version?: string; receipt_replay_event?: unknown; public_safe?: boolean; no_live_side_effects?: boolean; output_summary?: string; no_write_no_side_effect_statement?: string; target_repo?: string }; }
describe("MCP Runtime local/dev and repo read-only surface", () => {
  it("runtime health returns Build 11 read-only status", () => { const health = getRuntimeHealth(); expect(health.status).toBe("ok"); expect(health.local_dev_only).toBe(true); expect(health.production_ready).toBe(false); expect(health.live_write_capabilities).toBe(false); expect(health.live_read_capabilities).toBe(true); });
  it("runtime capabilities list safe tool families", () => { const capabilities = getRuntimeCapabilities(); expect(capabilities.tool_families).toEqual(["guard", "host", "mission", "policy", "registry", "repo"]); expect(capabilities.read_only_tools).toBe(true); expect(capabilities.live_write_tools).toBe(false); expect(capabilities.repo_status_read_only_tools).toEqual([...repoToolNames]); });
  it("tool listing includes expected App Host and repo/status tools", () => { const names = listRuntimeTools().map((tool) => tool.name); for (const name of MCP_RUNTIME_SAFE_TOOL_NAMES) expect(names).toContain(name); for (const name of repoToolNames) expect(names).toContain(name); });
  it("tool descriptors are read-only and never live-write/destructive", () => { expect(assertRuntimeToolInventorySafe(listRuntimeTools())).toBe(true); for (const tool of listRuntimeTools()) { expect(tool.annotations.readOnlyHint).toBe(true); expect(tool.safety.readOnly).toBe(true); expect(tool.safety.liveWrite).toBe(false); expect(tool.safety.externalSideEffect).toBe(false); expect(tool.safety.destructive).toBe(false); if (tool.repo_status_tool) expect(tool.safety.liveCapabilityStatus).toBe("read_only_live"); else expect(tool.safety.liveCapabilityStatus).toBe("disabled"); } });
  it("no live write/destructive/money/prod/browser/repo mutation tools are exposed", () => { for (const tool of listRuntimeTools()) { expect(tool.name).not.toMatch(mutationForbiddenName); expect(tool.safety.allowedEffects.join(" ")).not.toMatch(mutationForbiddenEffect); expect(tool.safety.liveWrite).toBe(false); expect(tool.safety.destructive).toBe(false); } });
  it("host.health call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("host.health"); expect(result.ok).toBe(true); expect((result.structuredContent as { status: string }).status).toBe("ok"); expect(receiptOf(result).no_live_side_effects).toBe(true); });
  it("host.capabilities call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("host.capabilities"); expect(result.ok).toBe(true); expect((result.structuredContent as { tools: unknown[] }).tools).toHaveLength(MCP_RUNTIME_SAFE_TOOL_NAMES.length); });
  it("mission.validate call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("mission.validate", { fixture: "normal_repo_feature" }); expect(result.ok).toBe(true); expect((result.structuredContent as { result: { valid: boolean } }).result.valid).toBe(true); });
  it("mission.compile call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("mission.compile", { fixture: "normal_repo_feature" }); expect(result.ok).toBe(true); expect((result.structuredContent as { result: { mission: { schema_version: string } } }).result.mission.schema_version).toBe("stealtheye-mission.v1"); });
  it("guard.evaluate.preview call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("guard.evaluate.preview", { fixture: "safe_repo_write" }); expect(result.ok).toBe(true); expect((result.structuredContent as { result: { decision: string } }).result.decision).toBe("allow"); expect(result._meta.preview_only).toBe(true); });
  it("registry.trust.preview call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("registry.trust.preview", { fixture: "trusted_manifest" }); expect(result.ok).toBe(true); expect((result.structuredContent as { result: { trusted: boolean } }).result.trusted).toBe(true); });
  it("policy.hard_stops call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("policy.hard_stops"); expect(result.ok).toBe(true); expect((result.structuredContent as { result: { categories: string[] } }).result.categories).toContain("raw_secrets_credentials"); });
  it("repo.status.summary call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("repo.status.summary"); expect(result.ok).toBe(true); expect((result.structuredContent as { result: { summary: string } }).result.summary).toContain("StealthEyeLLC/stealtheye-agent-os"); expect(receiptOf(result).target_repo).toBe("StealthEyeLLC/stealtheye-agent-os"); });
  it("repo.metadata.read call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("repo.metadata.read"); expect(result.ok).toBe(true); expect((result.structuredContent as { result: { default_branch: string } }).result.default_branch).toBe("main"); });
  it("repo.pr.list call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("repo.pr.list", { state: "all" }); expect(result.ok).toBe(true); expect((result.structuredContent as { result: unknown[] }).result.length).toBeGreaterThan(0); });
  it("repo.workflow.status.read call succeeds through runtime", async () => { const result = await createMcpRuntimeServer().callTool("repo.workflow.status.read", { ref: "main" }); expect(result.ok).toBe(true); expect((result.structuredContent as { result: unknown[] }).result.length).toBeGreaterThan(0); });
  it("repo write/commit/PR mutation names are rejected", async () => { await expect(createMcpRuntimeServer().callTool("repo.commit.live")).rejects.toThrow(/Unknown runtime tool/); await expect(createMcpRuntimeServer().callTool("repo.pr.create")).rejects.toThrow(/Unknown runtime tool/); await expect(createMcpRuntimeServer().callTool("repo.issue.update")).rejects.toThrow(/Unknown runtime tool/); });
  it("invalid input is rejected", async () => { await expect(createMcpRuntimeServer().callTool("mission.validate", { fixture: "not_a_fixture" })).rejects.toThrow(); });
  it("resource listing includes runtime and dev-mode resources", () => { const uris = listRuntimeResources().map((resource) => resource.uri); expect(uris).toContain(RUNTIME_RESOURCE_URIS.runtime); expect(uris).toContain(RUNTIME_RESOURCE_URIS.devMode); expect(uris).toContain(RUNTIME_RESOURCE_URIS.health); });
  it("read resource returns public-safe content", () => { const runtime = readRuntimeResource(RUNTIME_RESOURCE_URIS.runtime); expect(runtime.public_safe).toBe(true); expect((runtime.contents as { live_write_tools: boolean }).live_write_tools).toBe(false); });
  it("tool call receipt preview is generated", async () => { const result = await createMcpRuntimeServer().callTool("host.health"); expect(receiptOf(result).schema_version).toBe("stealtheye-mcp-runtime-receipt-preview.v1"); expect(receiptOf(result).receipt_replay_event).toBeTruthy(); });
  it("repo receipt preview is public-safe and side-effect-free", async () => { const result = await createMcpRuntimeServer().callTool("repo.metadata.read"); expect(receiptOf(result).public_safe).toBe(true); expect(receiptOf(result).no_write_no_side_effect_statement).toContain("No branch"); });
  it("dev-mode connection info is fixture/public-safe", () => { const info = createDevModeConnectionInfo(); expect(info.public_safe).toBe(true); expect(info.production_deployment).toBe(false); expect(info.oauth_credentials_provisioned).toBe(false); expect(info.live_write_tools).toBe(false); });
  it("transport exposes local/dev handlers without requiring network access", async () => { const transport = createLocalDevTransport(); expect(transport.local_dev_only).toBe(true); expect((await transport.handle({ method: "GET", path: "/health" })).status).toBe(200); expect((await transport.handle({ method: "POST", path: "/tools/call", body: { name: "host.health" } })).status).toBe(200); expect((await transport.handle({ method: "POST", path: "/tools/call", body: { name: "repo.metadata.read" } })).status).toBe(200); });
  it("no real secret-like material, customer data, or live write endpoints appear in fixtures/descriptors", () => { const serialized = JSON.stringify({ tools: listRuntimeTools(), resources: listRuntimeResources(), devMode: createDevModeConnectionInfo() }); expect(serialized).not.toMatch(secretLike); for (const name of MCP_RUNTIME_SAFE_TOOL_NAMES) expect(AppHostToolNameSchema.safeParse(name).success).toBe(true); });
});
