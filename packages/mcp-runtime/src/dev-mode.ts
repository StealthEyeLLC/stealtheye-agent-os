import { MCP_RUNTIME_ID } from "./fixtures";
import { McpRuntimeSchemaVersion } from "./schemas";
export function createDevModeConnectionInfo(runtime_id = MCP_RUNTIME_ID) {
  return {schema_version:McpRuntimeSchemaVersion.DevModeConnectionInfo,runtime_id,connector_name:"StealthEye Agent OS Dev Runtime",connector_description:"Local/dev runtime for safe StealthEye Agent OS planning, policy, registry, guard, mission previews, and public repo/status read-only observation.",endpoint_path:"/mcp",supported_protocols:["streaming_http","sse"],authentication_modes:["none_for_build_11"],public_distribution:false,production_deployment:false,oauth_credentials_provisioned:false,live_read_tools:true,live_write_tools:false,repo_status_read_only_tools:true,public_safe:true,required_later_steps:["Wire to an official MCP SDK server or compatible streaming transport.","Expose a reachable development endpoint if ChatGPT developer mode requires one."],notes:["Build 11 does not submit a public app, deploy production infrastructure, provision OAuth credentials, or expose live write tools.","Repo/status tools may read public repository metadata only when configured; CI uses fixtures and mocked fetch."]};
}
